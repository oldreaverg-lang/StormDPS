# Secret scanning before every commit.
# One-time setup:  pip install pre-commit  &&  pre-commit install
# Bump hook versions later with:  pre-commit autoupdate
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks
